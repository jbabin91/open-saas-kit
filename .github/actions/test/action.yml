name: Test
description: Run tests with coverage reporting

runs:
  using: composite
  steps:
    - name: Install Playwright browsers
      shell: bash
      run: pnpm --filter @oakoss/ui exec playwright install chromium --with-deps

    - name: Run tests with coverage
      shell: bash
      run: pnpm test:coverage
